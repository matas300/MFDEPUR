<%- include('../partials/_header', { title: 'Link non valido' }) %>

<div class="auth-container">
  <div class="auth-card" style="text-align:center">
    <div style="font-size:4rem;margin-bottom:1rem">❌</div>
    <h1 style="margin-bottom:0.5rem">Link scaduto o non valido</h1>
    <p style="color:var(--text-muted);margin-bottom:2rem">
      Il link di reset password non è valido o è scaduto (validità 1 ora).<br>
      Richiedi un nuovo link.
    </p>
    <a href="/auth/forgot-password" class="btn btn-primary">Richiedi nuovo link</a>
    <div style="margin-top:1rem">
      <a href="/auth/login" style="color:var(--text-muted)">Torna al login</a>
    </div>
  </div>
</div>

<%- include('../partials/_footer') %>
